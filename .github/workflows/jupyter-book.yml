name: Build and Deploy Jupyter Book

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      ready: ${{ steps.guard.outputs.ready }}
      pages_enabled: ${{ steps.pages_check.outputs.pages_enabled }}
    steps:
      - uses: actions/checkout@v5

      # Single source of truth — see .github/actions/check-ready.
      - name: Check repository readiness
        id: guard
        uses: ./.github/actions/check-ready

      - name: Check whether GitHub Pages is enabled
        id: pages_check
        if: steps.guard.outputs.ready == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh api "repos/${{ github.repository }}/pages" >/dev/null 2>&1; then
            echo "::notice::GitHub Pages is enabled — Jupyter Book will deploy."
            echo "pages_enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::GitHub Pages is not yet enabled on this repo. Build will run; deploy will be skipped. Enable Pages at https://github.com/${{ github.repository }}/settings/pages (Source: GitHub Actions) to enable deploy on the next push."
            echo "pages_enabled=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up pixi
        if: steps.guard.outputs.ready == 'true'
        uses: prefix-dev/setup-pixi@v0.9.6
        with:
          pixi-version: v0.68.1
          locked: true
          cache: true
          environments: docs

      - name: Convert .py notebooks to .ipynb
        if: steps.guard.outputs.ready == 'true'
        run: |
          for nb in notebooks/*.py; do
            pixi run jupytext --to notebook "$nb"
          done

      # Glob, not a hard-coded list — new notebooks are picked up automatically.
      - name: Execute notebooks
        if: steps.guard.outputs.ready == 'true'
        env:
          # CDSE credentials so 01 authenticates and the pipeline can fetch
          # granules. secrets ARE readable in a step `env:` (unlike a step `if:`)
          # — see the note in ci.yml. Undefined secrets just come through empty.
          CDSE_USERNAME: ${{ secrets.CDSE_USERNAME }}
          CDSE_PASSWORD: ${{ secrets.CDSE_PASSWORD }}
          CDSE_S3_ACCESS_KEY: ${{ secrets.CDSE_S3_ACCESS_KEY }}
          CDSE_S3_SECRET_KEY: ${{ secrets.CDSE_S3_SECRET_KEY }}
        run: |
          for nb in notebooks/*.ipynb; do
            echo "::group::Executing $nb"
            pixi run jupyter execute --inplace "$nb"
            echo "::endgroup::"
          done

      - name: Build MyST site
        if: steps.guard.outputs.ready == 'true'
        env:
          # MyST silently ignores `base_url` in myst.yml — only this env var
          # works. See docs/cicd-conventions.md.
          BASE_URL: /${{ github.event.repository.name }}
        run: pixi run -e docs myst build --html

      # The reader-facing story page rides the SAME Pages site as the book, served
      # at /blog/. Best-effort: skipped (never fails the book deploy) if the key is
      # absent. This is a dev-network replication, so the constellation + platform
      # hosts are the dev ones; re-running this workflow regenerates the blog.
      - name: Generate the reader-facing story page (best-effort)
        if: steps.guard.outputs.ready == 'true'
        continue-on-error: true
        env:
          SCIENCELIVE_API_KEY: ${{ secrets.SCIENCELIVE_API_KEY }}
          SCIENCELIVE_API: https://api-dev.sciencelive4all.org/np/constellation
          SCIENCELIVE_PLATFORM: https://platform-dev.sciencelive4all.org
        run: |
          if [ -z "$SCIENCELIVE_API_KEY" ]; then
            echo "::notice::SCIENCELIVE_API_KEY not set — skipping the story page (book deploys without /blog/)."
            exit 0
          fi
          if pixi run -e docs build-story; then
            mkdir -p _build/html/blog
            cp -r blog/. _build/html/blog/
            echo "::notice::Story page built — included on the Pages site at /blog/."
          else
            echo "::warning::build-story did not produce a page — deploying the book without /blog/."
          fi

      - name: Upload pages artifact
        if: steps.guard.outputs.ready == 'true'
        uses: actions/upload-pages-artifact@v5
        with:
          path: _build/html

  deploy:
    needs: build
    if: needs.build.outputs.ready == 'true' && needs.build.outputs.pages_enabled == 'true'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
